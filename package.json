{
  "name": "simulated-location-bar",
  "version": "0.3.4",
  "description": "a simple React component for use in simulating a location bar for embedded examples of react-router — eg. for use with CodePen or JSBin.",
  "main": "./src/LocationBar.js",
  "scripts": {
    "prepublish": "npm run build",
    "build": "babel ./src/LocationBar.js --out-file ./dist/LocationBar.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "files": [
    "dist"
  ],
  "author": "Adam Brown <adamarthurryan@gmail.com>",
  "license": "BSD-2-Clause",
  "dependencies": {
    "react": "^15.4.1",
    "react-dom": "^15.4.1",
    "react-router": "^3.0.0"
  },
  "devDependencies": {
    "babel-cli": "^6.18.0",
    "babel-preset-es2015": "^6.18.0",
    "babel-preset-react": "^6.16.0",
    "babel-preset-stage-2": "^6.18.0"
  }
}
